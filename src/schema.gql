# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Task {
  id: Float!
  title: String!
  description: String!
  status: String!
}

type User {
  id: Float!
  username: String!
  password: String!
}

type Query {
  whoAmI: User!
  tasks: [Task!]!

  """Returns all the task for a particular User"""
  getAllTask: [Task!]!

  """Returns all the task for a particular User by a given ID"""
  getTaskById(id: Int!): Task!
}

type Mutation {
  """Returns token after login"""
  signin(authCredentialsDTO: AuthCredentialsDTO!): User!

  """Creates a User Account"""
  Signup(authCredentialsDTO: AuthCredentialsDTO!): User!

  """Deletes a specific task for a particular User by a given ID"""
  deleteTaskById(id: Int!): Task!

  """Adds task to a Specific User's List of Tasks"""
  addTask(createTaskDTO: CreateTaskDTO!): Task!

  """Updates a  task for a Specific User's List of Tasks"""
  updateTask(id: Int!, status: TaskStatus!): Task!
}

input AuthCredentialsDTO {
  username: String!
  password: String!
}

input CreateTaskDTO {
  title: String!
  description: String!
}

enum TaskStatus {
  OPEN
  PENDING
  CLOSED
}
